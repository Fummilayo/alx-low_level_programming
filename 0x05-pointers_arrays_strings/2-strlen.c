#include "main.h"
/**
 * _strlen - a function that returns the length of a string.
 * @s: a pointer variable
 * Return: Always 0 (success)
 **/
int _strlen(char *s)
{ int length;
length = strlen(s);
}
