#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: 0 (success)
 */
int main(void)
{char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{ if(alphabet == 'q')
putchar(continue);
else if (alphabet == 'e')
putchar(continue);
}
return 0;
}
